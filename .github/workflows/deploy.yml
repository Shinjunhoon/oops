name: Build, Push, and Deploy (Docker Hub Strategy)

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      # ... (1. Docker Hub 로그인 및 2. 이미지 빌드/푸시 단계는 동일) ...
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/oops-app:latest


      # 3. EC2 서버에 SSH 명령으로 배포 시작 (Pull 명령만 실행)
      - name: Deploy to EC2 (Secure Pull and Run)
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          timeout: 10m

          script: |
            REPO_DIR="/home/${{ secrets.EC2_USER }}/spring-boot-app"
            
            # 4-1️⃣ 리포지토리 파일 업데이트
            if [ ! -d "$REPO_DIR" ]; then
              echo "Cloning repository..."
              git clone https://github.com/${{ github.repository }}.git "$REPO_DIR"
            else
              echo "Pulling latest changes..."
              cd "$REPO_DIR"
              git pull origin main
            fi
            
            cd "$REPO_DIR"
            
            # 4-2️⃣ 민감한 환경 변수 문자열 생성 (평문 파일 저장 없이)
            # ⚠️ 모든 비밀 변수를 **싱글 쿼트('...')로 감싸** 특수문자 문제를 방지합니다.
            ENV_VARS="DOCKER_USERNAME='${{ secrets.DOCKER_USERNAME }}' \
                      AWS_ACCESS_KEY='${{ secrets.AWS_ACCESS_KEY }}' \
                      AWS_SECRET_KEY='${{ secrets.AWS_SECRET_KEY }}' \
                      AWS_RDS_USERNAME='${{ secrets.AWS_RDS_USERNAME }}' \
                      AWS_RDS_PASSWORD='${{ secrets.AWS_RDS_PASSWORD }}' \
                      MAIL_PASSWORD='${{ secrets.MAIL_PASSWORD }}' \
                      JWT_SECRET='${{ secrets.JWT_SECRET }}' \
                      AWS_RDS_ENDPOINT='${{ secrets.AWS_RDS_ENDPOINT }}' \
                      AWS_RDS_DATABASE='${{ secrets.AWS_RDS_DATABASE }}'" # 💡 이전 단계에서 제안한 RDS 변수 추가
            
            echo "Environment variables prepared for non-persistent injection."
            
            # 4-3️⃣ 최신 Docker 이미지 Pull (ENV_VARS를 앞에 붙여 실행)
            echo "Pulling latest image from Docker Hub..."
            # ENV_VARS로 변수를 주입하여 docker-compose.yml의 ${DOCKER_USERNAME}을 해석합니다.
            $ENV_VARS docker-compose pull app
            
            # 4-4️⃣ 컨테이너 재시작
            echo "Starting containers..."
            # ENV_VARS로 변수를 주입하여 컨테이너 내부의 환경 변수 및 RDS URL을 설정합니다.
            $ENV_VARS docker-compose up -d --force-recreate
            
            echo "Deployment complete. Sensitive variables were not persisted."