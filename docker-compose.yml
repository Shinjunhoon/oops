version: '3.8'

services:

  redis:
    image: redis:latest
    container_name: oops_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - backend

  app:
    # 🚨 변경: EC2 빌드 옵션을 제거합니다.
    # build: .

    # 🚨 변경: Docker Hub에서 이미지를 가져오도록 경로를 지정합니다.
    image: ${DOCKER_USERNAME}/oops-app:latest

    container_name: spring_app_container
    restart: always
    ports:
      - "8081:8080"
    depends_on:
      - redis
    environment:
      - SPRING_REDIS_HOST=redis
      - SPRING_REDIS_PORT=6379

      # RDS 접속 정보 및 기타 Secret 환경 변수
      - AWS_ACCESS_KEY=${AWS_ACCESS_KEY}
      - AWS_SECRET_KEY=${AWS_SECRET_KEY}
      - AWS_RDS_USERNAME=${AWS_RDS_USERNAME}
      - AWS_RDS_PASSWORD=${AWS_RDS_PASSWORD}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - JWT_SECRET=${JWT_SECRET}
      - RDS_URL=${RDS_URL}
      - CLIENT_ID=${CLIENT_ID}
      - CLIENT_SECRET=${CLIENT_SECRET}

      # ⚠️ RDS URL은 Spring Boot 설정 파일에 있거나, 여기에 명시되어야 합니다.
      # Spring Boot 설정 파일에 ${...} 변수만 있다면, 여기에 명시해 주세요.
      # 예시: - SPRING_DATASOURCE_URL=jdbc:mysql://[RDS 엔드포인트]:3306/...

    networks:
      - backend

volumes:
  redis_data:

networks:
  backend:
    driver: bridge